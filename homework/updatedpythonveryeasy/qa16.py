#  Write a Python program to check if a string is numeric.

#  This method is present only on unicode objects.
# to define a string as unicode , one simply prefixes a "u" to the 
#  quotation mask of the assignment.

x = u"priyanka";
print(x.isnumeric())

x = u"742";
print(x.isnumeric())
